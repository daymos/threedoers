.row#header
    .container
        ul.nav-circles
            - if (user && user.filemanager == 'accepted' || user.printer != 'accepted')
            li.tiles
                a(href="/project/upload")
                    img(src='/img/icons_printing.png').circular.small-circular
                div.description-to-hide
                    h4 Send to Print
                    p
                        | Upload an STL file,
                        | see price and order
                        | your print

            - if (user && user.printer == 'accepted' || user.filemanager != 'accepted')
            li.tiles
                a(data-toggle="modal",data-target="#comingsoonModal",href="#")
                    img(src='/img/icon-1.png').circular.small-circular
                div.description-to-hide
                    h4 Find a Designer
                    p
                        | Describe your project
                        | and see the best
                        | Designer for you

            - if (user)
            li.tiles
                a(href='/profile/settings')
                    img(src='/img/icon-22.png').circular.small-circular
                div.description-to-hide
                    h4 Upgrade your Profile
                    p
                        | Are you a Printer or a Designer?
                        | Click here to Upgrade your profile
                        | and start print or design for other



include ../modals/coming-soon
include ../modals/become-printer
