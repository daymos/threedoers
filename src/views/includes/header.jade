.row
 header
  #header
    unless user
      .container
        .row
          .col-md-6
            a(href='/')
              img(src='/img/logo.png')
          .col-md-6
            nav
              .main-nav
                ul
                  li
                    a(href='/accounts/login') LOG IN
                  li
                    a(href='/accounts/signup') SIGN UP
    else
       .container
        .row
            .clearfix
            .simple-nav
              nav
                ul.nav.nav-pills.col-md-offset-3

                  //- if (user && user.printer == 'accepted')
                  //  li
                  //    a(href='/printing/requests') Available Jobs
                  //- if (user && user.filemanager == 'accepted')
                  //  li
                  //    a(href='/design/requests') Available Files
                  - if (user && user.filemanager != 'accepted' || user.printer != 'accepted')
                    li
                      a(href='/project/upload') Send to Print

                  //- if (user && user.printer != 'accepted')
                  //  li
                  //    a(href='/become') Become A Printer
                  //- if (user && user.filemanager != 'accepted')
                  //  li
                  //    a(href='/become/filemanager') Become A Designer
                  - if (user && user.filemanager != 'accepted')
                    li
                      a(href='/design/stl') Find a Designer
                  - if (user)
                    li
                        a(href='/design/stl') Upgrade Profile

            .simple-nav.description-to-hide
                nav
                    ul.nav.nav-pills.col-md-offset-3
                        - if (user && user.filemanager != 'accepted' || user.printer != 'accepted')
                            li
                                p some ugly text
                        - if (user && user.filemanager != 'accepted')
                            li
                                p some ugly text
                        - if (user)
                            li
                                p some ugly text
