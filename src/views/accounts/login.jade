extends base

block title
  |  Login

block content
  .row
    div.well.col-md-5.center-block
      form(method="post", action="/accounts/login", role="form", id="loginForm")
        h2 Please Login
        br
        - if (locals.error)
          div(class="alert alert-danger")
            = error

        input(type='hidden', name='_csrf', value=csrfToken)

        .form-group(class=(locals.errors && errors.username)?"has-error":"")
          label(for="username", class="control-label") Username
          input(type="text", class="form-control", name="username", id='username', placeholder="Enter your username", value=(locals.username)?"#{username}":"").form-control
          - if (locals.errors && errors.username)
            span(class="help-block")
              = errors.username.msg

        .form-group(class=(locals.errors && errors.password)?"has-error":"")
          label(for="password", class="control-label") Password
          input(type="password", class="form-control", name="password", id='password', placeholder="Enter your password", value=(locals.password)?"#{password}":"").form-control
          - if (locals.errors && errors.password)
            span(class="help-block")
              = errors.password.msg

        button(type="submit").btn.btn-primary.btn-lg.btn-block
          span.glyphicon.glyphicon-lock
          | &nbsp;&nbsp;&nbsp;Sign in

        .checkbox
          label
            input(type="checkbox", checked='checked')
            | Remember Me

        div.btn-links
          a(href='#')#forgot-password Forgot Your Password?
          br
          a(href='/accounts/signup')#create-account Create An Account

  include ../modals/alert
  include ../modals/lost-password

block scripts
  script(src='/js/views/login.js')